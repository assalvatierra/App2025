import { TablePosition } from '../../../../common/model/tables/main-structures/table';
import { TableCell } from '../../../../common/model/tables/main-structures/table-cell';
import { IProcessor } from '../../../../common/processor';
import { SubDocument } from '../../../../common/model/sub-document';
import { TableCellApi } from '../../table/table-cell';
import { TableRowApi } from '../../table/table-row';
import { TableBaseCollection } from './table-base-collection';
export declare class TableCellCollection extends TableBaseCollection<TableCellApi, TableCell> {
    private _tablePosition;
    private _parentRow?;
    constructor(processor: IProcessor, subDocument: SubDocument, tablePosition: TablePosition, parentRow?: TableRowApi);
    protected _getItem(coreItem: TableCell): TableCellApi;
    protected _getCoreItems(): TableCell[];
    insert(index: number, toRight?: boolean): TableCellApi;
    remove(index: number): void;
}
