import { CharacterDecoder } from './character-decoder';
export class EmptyCharacterDecoder extends CharacterDecoder {
    constructor() {
        super();
    }
    processChar(importer, ch) {
        importer.destination.processChar(ch);
    }
    flush(_importer) {
    }
}
