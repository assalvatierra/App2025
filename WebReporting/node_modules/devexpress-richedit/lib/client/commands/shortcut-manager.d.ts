import { CommandManager } from '../../common/commands/command-manager';
import { ShortcutManager } from '../../common/commands/shortcut-manager';
import { IRichEditControl } from '../../common/interfaces/i-rich-edit-core';
export declare class ClientShortcutManager extends ShortcutManager {
    constructor(commandManager: CommandManager, control: IRichEditControl);
}
