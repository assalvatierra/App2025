export declare class TableStyleOptionsApi {
    headerRow: boolean;
    totalRow: boolean;
    bandedRows: boolean;
    firstColumn: boolean;
    lastColumn: boolean;
    bandedColumns: boolean;
    constructor(headerRow: boolean, totalRow: boolean, bandedRows: boolean, firstColumn: boolean, lastColumn: boolean, bandedColumns: boolean);
}
export declare const TABLE_STYLE_OPTIONS_KEYS: string[];
