import { RtfImportData } from '../rtf-import-data';
import { CharacterDecoder } from './character-decoder';
export declare class UnicodeCharacterDecoder extends CharacterDecoder {
    private static readonly capacity;
    private _chars;
    processChar(importer: RtfImportData, ch: string): void;
    flush(importer: RtfImportData): void;
    protected canAppendText(importer: RtfImportData): boolean;
    private flushByChar;
    private flushByString;
}
