import { TablePosition } from '../../../common/model/tables/main-structures/table';
import { SubDocument } from '../../../common/model/sub-document';
import { IProcessor } from '../../../common/processor';
import { TableCellCollection } from '../collections/table/table-cell-collection';
import { IntervalApi } from '../interval';
import { TableApi } from './table';
import { TableRowHeight } from './types';
import { TableElementBase } from './table-element-base';
export declare class TableRowApi extends TableElementBase {
    private _tablePosition;
    private _parentTable;
    constructor(processor: IProcessor, subDocument: SubDocument, tablePosition: TablePosition, parentTable?: TableApi);
    get index(): number;
    get interval(): IntervalApi;
    get cells(): TableCellCollection;
    get parentTable(): TableApi;
    get height(): TableRowHeight;
    set height(value: TableRowHeight);
}
