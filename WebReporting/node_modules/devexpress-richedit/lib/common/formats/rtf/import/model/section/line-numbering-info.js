export class RtfLineNumberingInfo {
    copyFrom(obj) {
        this.distance = obj.distance;
        this.start = obj.start;
        this.countBy = obj.countBy;
        this.restart = obj.restart;
    }
}
