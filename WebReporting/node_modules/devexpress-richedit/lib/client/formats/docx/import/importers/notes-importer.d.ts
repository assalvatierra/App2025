import { Data } from "../data";
export declare abstract class NotesImporter {
    data: Data;
    notes: Record<string, number>;
    separatorIds: string[];
    constructor(data: Data);
    abstract insertRun(subDocId?: number): void;
}
