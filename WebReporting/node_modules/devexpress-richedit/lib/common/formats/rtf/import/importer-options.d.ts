import { InvalidDocumentMode } from '../../utils/enums';
export declare class RtfImporterOptions {
    throwInvalidFile: (reason: string) => void;
    invalidDocumentMode: InvalidDocumentMode;
    enableNotCompatibleUIFunctionality: boolean;
    constructor(throwInvalidFile?: (reason: string) => void);
}
